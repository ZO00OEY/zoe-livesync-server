# Zoe LiveSync Server

- 默认使用中文编写说明和终端提示。
- `install.sh` 必须保持可重复执行；不得静默停止、删除或覆盖无关服务。
- CouchDB 只能绑定到回环地址，公网仅开放 Caddy 的 80/443。
- 所有用户输入在写入配置前都必须校验。
- 不得把真实凭据、证书私钥或生成的 `.env` 提交到 Git。
- 修改安装流程后，至少运行 Bash 语法检查和脚本自测。
